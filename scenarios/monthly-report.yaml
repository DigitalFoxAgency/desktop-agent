schemaVersion: 1
id: monthly-report
version: 0.1.0
name: Monthly Report (Phase 2)
description: >
  Recurring optimisation pass and monthly report for an active client.

inputs:
  - name: clientPath
    kind: path
    required: true
    description: Absolute path to the client folder.

steps:
  - module: df-client-launchpad
    skill: reporting
    description: Optimisation pass and monthly client report.
    inputs:
      clientPath: $input.clientPath
