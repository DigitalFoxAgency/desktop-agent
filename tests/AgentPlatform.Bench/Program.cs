// AgentPlatform.Bench — BenchmarkDotNet runner.
//
// Benches are added in Phase 8 T148 (run-container cold start, bridge
// round-trip, hand-off propagation, file-tree update latency). Until
// then this is just a placeholder so the project builds.

Console.WriteLine("AgentPlatform.Bench: no benchmarks yet — see tasks.md T148.");
return 0;
